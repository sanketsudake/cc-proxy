apiVersion: 1

providers:
  - name: cc-proxy
    folder: Claude Agent Proxy
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
